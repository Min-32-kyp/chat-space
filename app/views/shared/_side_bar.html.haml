.sideSpace
  .sideSpace__header
    %h3.sideSpace__header__user
      = current_user.name
    %ul.sideSpace__header__menu
      %li.list
        = link_to '#' do
          = fa_icon 'pencil-square-o', class: 'icon'
      %li.list
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: 'icon'
  .sideSpace__body
    .sideSpace__body__group
      .sideSpace__body__group__name
        グループ名(その１)
      .sideSpace__body__group__lastMessage
        最後のメッセージ(おはよう)
    .sideSpace__body__group
      .sideSpace__body__group__name
        グループ名(その２)
      .sideSpace__body__group__lastMessage
        最後のメッセージ(テスト)
    .sideSpace__body__group
      .sideSpace__body__group__name
        グループ名(その３)
      .sideSpace__body__group__lastMessage
        最後のメッセージ(おやすみ)